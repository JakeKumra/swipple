import React from "react"

export default function Exchange() {
    // this page needs to have a conditional render? 
    // if the user is authenticated, give them an option to post an item
    // else request the user signs in
    // fix this page
    return (
        <>
            <h1>This is the Exchange page</h1>
        </>
    ) 
}